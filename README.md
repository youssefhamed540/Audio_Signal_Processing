# Audio Signal Processing & Visualization Pipeline

Generates a sample audio signal, analyzes it in the time and frequency domains,
and optionally supports WAV files.

## Run
```bash
pip install numpy matplotlib soundfile librosa
python main.py
```

The script creates waveform, spectrogram, and FFT plots inside `outputs/`.
